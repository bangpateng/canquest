# ─── Dependencies ────────────────────────────────────────────────────────────
**/node_modules/
.pnp
.pnp.*

# ─── Build outputs ───────────────────────────────────────────────────────────
**/.next/
**/dist/
**/out/
**/build/
**/*.tsbuildinfo

# ─── Environment / Secrets ───────────────────────────────────────────────────
# ALL .env files are secret — only .env.example files are safe to commit.
**/.env
**/.env.*
!**/.env.example
!**/.env.*.example

# infra/env/ — real values, VPS copy-paste, references (NEVER commit)
infra/env/*
!infra/env/*.example
infra/env/*COPY*
infra/env/*VPS*
infra/env/*.reference
infra/env/*.txt
infra/env/*.production
infra/env/*.staging
infra/env/*.devnet
infra/env/*.local
infra/env/*.md

# ─── Infra ops docs (IPs, deploy steps, passwords in notes — local only) ─────
infra/**/*.md
work.md
*.env.md

# ─── WireGuard (live config & keys on VPS only; *.example is safe template) ──
infra/wireguard/*
!infra/wireguard/*.example
**/wireguard/*.key
**/*_private.key
**/*_public.key

# ─── Ops runbooks with VPS IPs / tunnel steps (local copy only) ──────────────
docs/WIREGUARD*.md
docs/LANGKAH_STABILKAN_TUNNEL_CANTON.md
docs/SESSION_HANDOFF.md

# ─── Local-only ops scripts (contain VPS IP / tunnel setup) ──────────────────
infra/vps2-setup-tunnels.sh
infra/systemd/*
!infra/systemd/canton-tunnel.service.example
infra/deploy.sh
infra/redeploy.sh
infra/cleanup.sh

# ─── SSH / TLS / Keys ────────────────────────────────────────────────────────
**/*.pem
**/*.key
**/*.crt
**/*.p12
**/*.pfx
**/id_rsa
**/id_ed25519
**/*_tunnel
**/*_tunnel.pub

# ─── Database ────────────────────────────────────────────────────────────────
**/prisma/*.db
**/prisma/*.db-journal
**/prisma/dev.db

# ─── Logs ────────────────────────────────────────────────────────────────────
**/npm-debug.log*
**/yarn-debug.log*
**/yarn-error.log*
**/.pnpm-debug.log*
**/logs/
**/*.log

# ─── DAML build cache ────────────────────────────────────────────────────────
packages/daml/.daml/

# ─── Uploads / Media ─────────────────────────────────────────────────────────
apps/web/public/quest-media/*
!apps/web/public/quest-media/.gitkeep
apps/api/uploads/**
!apps/api/uploads/.gitkeep
!apps/api/uploads/avatars/.gitkeep

# ─── Vercel ──────────────────────────────────────────────────────────────────
.vercel

# ─── OS / Editor ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo
nul
# Windows shell artefacts (Git Bash redirect `> nul`, Cygwin/MSYS crash dumps)
*.stackdump

# ─── Internal / infra docs (host IPs, deploy runbooks, investigation notes) ──
# Keep these LOCAL only — repo is public. They leak infrastructure topology.
docs/CANTON_KONEKSI_VPS_GRPC_VS_JSON.md
docs/CANTON_STABLE_CONNECTION.md
docs/CANTON_TESTNET.md
docs/NETWORK_TOPOLOGY.md
docs/GUIDE_DEPLOY_FULL.md
docs/TUTORIAL_DEPLOY_INDONESIA.md
docs/VPS2_DEPLOY.md
docs/VPS2_OPERATIONS_RUNBOOK.md
docs/DAML_DEPLOY_VPS.md
docs/DAML_SDK_SETUP.md
docs/MAINNET_DEPLOY_CANQUEST_v10.md
docs/MAINNET_MIGRATION_GUIDE.md
docs/PERFORMANCE_API.md
apps/api/env.example.txt
dokumentasi.txt
probe-out-preapproval-choice-argument.json
diff_auth.txt
diff_party.txt
CREATE_PREAPPROVAL_LEDGER_INVESTIGATION.md
ONE_STEP_TOGGLE_AUDIT.md
PREAPPROVAL_LEDGER_MIGRATION_PLAN.md
QUESTS_SERVICE_MIGRATION_DIFF.md
REWARD_CIP56_MIGRATION_ANALYSIS.md

# ─── Infra config & ops scripts with host IPs (local only) ───────────────────
infra/nginx/canquest-api.conf
infra/nginx/canquest.conf
infra/systemd/canton-tunnel.service.example
infra/env/api.env.testnet.example
scripts/tunnel-testnet.ps1
scripts/vps2-deploy-from-windows.ps1
.zcode/
