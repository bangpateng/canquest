sdk-version: 3.3.0
# Package name canquest-v3 — module Main (v1.1.0)
# v1.0.0: UserAccount, Mission, DailyLuckySpin
# v1.1.0: UserAccount (earnedPoints+spentPoints+DebitSpinCost),
#          Mission, SpinExecution, SpinCcReward
# Templates baru: SpinExecution (audit on-chain per spin), SpinCcReward (bukti CC delivery)
# Test:      testFullWorkflow (Daml.Script)
name: canquest-v3
source: daml
version: 1.1.0
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
# Canton Network documentation reference:
# https://docs.canton.network/appdev/modules/m3-building-packaging
