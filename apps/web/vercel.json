{
  "framework": "nextjs",
  "regions": ["sin1"],
  "installCommand": "cd ../.. && npm install --ignore-scripts && cd apps/web && npm install --ignore-scripts",
  "crons": [
    {
      "path": "/api/health",
      "schedule": "*/5 * * * *"
    }
  ]
}
